{
  "name": "auto-header-ids-toc",
  "displayName": "Auto Header IDs and TOC",
  "publisher": "Ravi S. Iyer",
  "description": "HTML Table of Contents (TOC) generator; Blogger post TOC generator",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:auto-header-ids-toc.addIds",
    "onCommand:auto-header-ids-toc.markHeadersAsNoTOC",
    "onCommand:auto-header-ids-toc.createTOC"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "auto-header-ids-toc.addIds",
        "title": "Auto Header IDs and TOC: Add IDs to Headers"
      },
      {
        "command": "auto-header-ids-toc.markHeadersAsNoTOC",
        "title": "Auto Header IDs and TOC: Mark Configured Headers as no-toc"
      },
      {
        "command": "auto-header-ids-toc.createTOC",
        "title": "Auto Header IDs and TOC: Create Table of Contents"
      }
    ],
    "configuration": {
      "title": "Auto Header IDs and TOC Configuration",
      "properties": {
        "auto-header-ids-toc.headersToProcess": {
          "type": "array",
          "default": [
            "1",
            "2"
          ],
          "items": {
            "type": "string",
            "enum": [
              "1",
              "2",
              "3",
              "4",
              "5",
              "6"
            ]
          },
          "description": "Specify which header elements (e.g., h1, h2) should have IDs added. Enter the numbers as strings in an array."
        }
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ravisiyer/html-auto-header-ids"
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.103.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
